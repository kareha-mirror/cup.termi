# termiの色コード

[英語版 (English version)](color.md)

端末の色のモードは主に **16色**、 **256色**、 **TrueColor** の3つあり、それぞれ色の指定方法が異なる。
端末ライブラリの **termi** では、それらの指定方法の違いを吸収し、どの方法で指定されても現在の端末の状態に合わせて近い色が指定される。

また、端末で表示する文字は前景色と背景色の組み合わせを指定することができ、termiではその2つをカンマ `,` で区切って併記する。

## 16色モード

16色モードでは色は `0` から `15` までの番号か、 **色名** で指定する。
番号と色名の対応は次の通り。

* 0: black
* 1: red
* 2: green
* 3: yellow
* 4: blue
* 5: magenta
* 6: cyan
* 7: white
* 8: bright black
* 9: bright red
* 10: bright green
* 11: bright yellow
* 12: bright blue
* 13: bright magenta
* 14: bright cyan
* 15: bright white

`default` という特別な名前はその端末のデフォルトの色を指定する。

色名の表記は単語を何で区切ってもよい。
例えば、次のものはすべて `10` 番の bright green になる。

* bright green
* bright-green
* brightgreen
* Bright Green
* BrightGreen

## 256色モード

256色モードでは色は `0` から `255` までの番号で指定する。
このうち `0` から `15` は16色モードの色に対応する。

次のコマンドを実行すると256色モードの色見本を表示する。

```text
termi color
```

`16` から `231` はカラーパレット、 `232` から `255` はグレースケールになっている。
詳細は色見本を参照してほしい。

## TrueColorモード

TrueColorモードでは、 **RGB** をそれぞれ2桁の **16進数** で表記した6桁の値を指定する。
例えば赤 `ff`、緑 `80`、青 `40` の場合は次のように書く。

```text
ff8040
```

## 前景色と背景色の併記

例えば前景色が `252`、背景色が `235` を指定する場合は次のように書く。

```text
252,235
```
